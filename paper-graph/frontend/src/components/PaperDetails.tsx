import type { PaperView } from '../types/paper'
export function PaperDetails({ paper, onSetSeed, onGenerate, onAdd }: { paper: PaperView; onSetSeed: (id: string) => void; onGenerate: (id: string) => void; onAdd: (id: string) => void }) { return <div className="paper-details-actions"><button type="button" onClick={() => onGenerate(paper.id)}>Generate graph</button><button type="button" className="seed-action" title="加入 Seed 列表，之后可用于 Build Seed Map" onClick={() => onSetSeed(paper.id)}>Add to seed list</button><button type="button" title="将论文及其关联文献追加到当前图谱" onClick={() => onAdd(paper.id)}>Add to current map</button></div> }
